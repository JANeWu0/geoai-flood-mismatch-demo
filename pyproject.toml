[project]
name = "geoai-flood-mismatch-demo"
version = "0.1.0"
description = "GeoAI demo for diagnosing flood response-impact spatial mismatch using CV, LLM signals, and SMI."
requires-python = ">=3.10"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Your Name"}]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
